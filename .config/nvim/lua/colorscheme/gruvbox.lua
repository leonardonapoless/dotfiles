-- Gruvbox Material (secondary theme)
-- Settings are applied by colorscheme/init.lua when this theme is selected.
-- This file only provides the Lazy.nvim plugin spec to install the plugin.
return {
    "sainnhe/gruvbox-material",
    lazy = true,
    priority = 1000,
}
